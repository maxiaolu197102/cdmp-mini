这些概念的核心是 **“不同层面的字符表示方式”**，从底层存储到人类可读的字符，再到编码规则，逐层递进。梳理如下：
一、最底层：字节（byte）与字节切片（[] byte）
byte：Go 中的基本类型，本质是 uint8 的别名，代表一个 8 位二进制数（0-255），是计算机存储数据的最小单位。例：b := byte(0x61) 等价于 b := 97，对应 ASCII 中的字符 'a'。
[]byte：字节切片，是 byte 的集合，代表一串原始二进制数据（未经过字符编码解析）。例：[]byte("abc") 存储的是 [97, 98, 99]（ASCII 编码的字节值）。
核心作用：计算机底层只认识字节，所有数据（包括字符串）最终都以 [] byte 形式存储或传输。
二、字符层面：Unicode 码点（rune）与 rune 切片（[] rune）
Unicode 码点（Code Point）：是全球字符的唯一编号（用 U+XXXX 表示），涵盖几乎所有语言的字符。例：'a' 的码点是 U+0061，'中' 的码点是 U+4E2D，😀 的码点是 U+1F600。
rune：Go 中的基本类型，本质是 int32 的别名，专门用来表示一个 Unicode 码点（范围 0-0x10FFFF）。例：r := '中' 等价于 r := rune(0x4E2D)，直接对应码点 U+4E2D。
[]rune：rune 的集合，代表一串 Unicode 字符（每个元素是一个独立字符的码点）。例：[]rune("a中😀") 存储的是 [0x61, 0x4E2D, 0x1F600]，对应 3 个字符。
核心作用：统一表示全球所有字符，解决 “不同编码体系混乱” 的问题（如 ASCII、GBK、UTF-8 等）。
三、字符串（string）：对字节的 “包装”
string：Go 中的字符串是只读的字节切片（[] byte），但语义上代表 “一串字符”。例：s := "a中😀" 底层存储的是 UTF-8 编码的字节序列：[97, 0xE4, 0xB8, 0xAD, 0xF0, 0x9F, 0x98, 0x80]（共 8 个字节）。
关键特性：
字符串的 “长度”（len(s)）是底层字节数，而非字符数（如 "中" 的 len 是 3，因为 UTF-8 编码占 3 字节）。
字符串可直接转换为 [] byte（[]byte(s)），得到底层字节；也可转换为 [] rune（[]rune(s)），得到每个字符的码点（自动按 UTF-8 解析）。
四、编码规则：UTF-8 与 Unicode 的关系
Unicode 是 “字符集”：定义了每个字符的唯一码点（如 U+4E2D 对应 “中”），但不规定如何存储这些码点。
UTF-8 是 “编码方式”：是 Unicode 码点的一种具体存储实现，用1-4 个字节表示一个码点，兼容 ASCII（单字节）。例：
码点 U+0061（'a'）→ UTF-8 编码为 0x61（1 字节）；
码点 U+4E2D（' 中 '）→ UTF-8 编码为 0xE4 0xB8 0xAD（3 字节）；
码点 U+1F600（'😀'）→ UTF-8 编码为 0xF0 0x9F 0x98 0x80（4 字节）。
Go 中的字符串默认用 UTF-8 编码：所以 string 与 []byte 的转换，本质是 UTF-8 字节序列的直接映射；string 与 []rune 的转换，本质是按 UTF-8 规则解析字节为 Unicode 码点。
五、关系总结：从存储到字符的 “解析链”
plaintext
计算机存储 → []byte（原始字节，如 UTF-8 编码）→ string（只读字节，语义为字符）
                                  ↓（按 UTF-8 解析）
                          []rune（每个元素是 Unicode 码点，即单个字符）
                                  ↓（映射）
                          Unicode 字符（如 'a'、'中'、'😀'）
六、记忆技巧
byte/[]byte：底层存储，看 “字节数”（len 直接得字节数）。
rune/[]rune：字符个体，看 “字符数”（len 得字符个数）。
string：是 UTF-8 编码的字节串，len 是字节数，转 []rune 才能得字符数。
Unicode：字符的 “身份证号”（码点），UTF-8：将 “身份证号” 写成 “存储地址” 的规则（1-4 字节）。
通过 “存储（字节）→ 编码（UTF-8）→ 字符（码点）” 的层级关系，就能理清它们的联系了